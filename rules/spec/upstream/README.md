# Vendored Instrumentation Score specification

Verbatim copies of `specification.md` and `rules/*.md` from
<https://github.com/instrumentation-score/spec>, pinned at the commit recorded
in `/.instrumentation-score-version`. Apache-2.0 (LICENSE included).

Do not edit these files. To bump: update the pin, re-fetch, review rule
diffs, and adjust the YAML implementations in `rules/spec/` accordingly.
